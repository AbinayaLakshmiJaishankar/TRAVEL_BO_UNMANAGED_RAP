projection;
strict ( 2 );
use draft;

define behavior for zc_travel_bo_unmanaged //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Edit;
  use action Resume;
  use action Discard;
  use action Prepare;

  use association _Booking { create; with draft; }
}

define behavior for zc_booking_bo_unmanaged //alias <alias_name>
{
  use update;
  use delete;

  use association _Travel { with draft; }
}